# ── Region & Project ──────────────────────────────
AWS_REGION=us-east-1
PROJECT_NAME=novamart-agentcore

# ── Bedrock Knowledge Bases (filled in Phase 8, AWS Console) ──
RETURNS_KB_ID=LZPEAC6SRC
SHIPPING_KB_ID=DLNCFH00LK
WARRANTY_KB_ID=PH5KRJSQJZ

# ── AgentCore Runtime (filled after `make deploy`, Phase 6) ──
#AGENTCORE_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:498078578439:runtime/novamart-agentcore-runtime-demo
AGENTCORE_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:498078578439:runtime/novamart_agentcore_runtime-H2x9ZEEX5C

# ── Bedrock Guardrail (filled after `make deploy`, Phase 6) ──
GUARDRAIL_ID=pwubrb8o8woq
GUARDRAIL_VERSION=1

# ── Local-testing override (AWS Academy/Vocareum Marketplace restriction) ──
# Uncomment ONLY if your account cannot invoke Anthropic models via Bedrock.
#ORCHESTRATOR_MODEL_ID=amazon.nova-lite-v1:0
#WORKER_MODEL_ID=amazon.nova-pro-v1:0